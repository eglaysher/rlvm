
Import('component_env')

component_env.Prepend(
  CPPPATH = [
    "#/vendor/glew/include/",
  ],
)

libglew_files = [
  "src/glew.c"
]

component_env.ParseConfig("sdl-config --cflags --libs")

component_env.StaticLibrary('libglew', libglew_files)
component_env.Install('$LIBRARY_DIR', 'libglew.a')
