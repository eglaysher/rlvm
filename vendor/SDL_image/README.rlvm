This is SDL_image 1.2.10, set up to compile only GIF, BMP, and PNG files. No
modifications have been made to the source; compile options are controlled with
#defines.

In addition, we take advantage of the ImageIO loading support for Mac OSX on
that platform.
