#if $(not $(defined-env VERBOSE))
#  OMakeFlags(-S --progress)
#  export

RLC = rlc
KPRL = kprl

.PHONY: clean
.SUBDIRS: jumpTest farcallTest_0 farcall_withTest

KEPAGOFILES = $(glob *.ke)

%.TXT: %.ke
	$(RLC) $< -o seen0001.TXT
	$(KPRL) -a $@ seen0001.TXT
	$(RM) seen0001.TXT

.DEFAULT: $(replacesuffixes .ke, .TXT, $(KEPAGOFILES)) jumpTest farcallTest_0 farcall_withTest

clean:
  $(RM) *~ *.TXT *.TXT.uncompressed
