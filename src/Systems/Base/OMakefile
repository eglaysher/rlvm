#if $(not $(defined-env VERBOSE))
#  OMakeFlags(-S --progress)
#  export

CXXFLAGS = --ansi -U__STRICT_ANSI__ -funsigned-char -Wall $(if $(defined-env DEBUG), -g, -O2) -I. -I../ -I../../ $(PLATFORMFLAGS) -D_THREAD_SAFE

# for testing
#CXXFLAGS += -DWIN32

.PHONY: clean

########

LIBS += -L/opt/local/lib
.DEFAULT:
#libSystems$(EXT_LIB)

clean:
  $(RM) *.o *.gch *$(EXT_LIB) *.omc
